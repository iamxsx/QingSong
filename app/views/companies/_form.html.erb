<%= form_for(company, multipart: true) do |f| %>
    <% if company.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(company.errors.count, "error") %> prohibited this company from being saved:</h2>

          <ul>
            <% company.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :company_name %>
      <%= f.text_field :company_name %>
    </div>

    <div class="field">
      <%= f.label :company_desc %>
      <%= f.text_area :company_desc %>
    </div>

    <div class="field">
      <%= f.label :company_logo %>
      <%= f.file_field :company_logo %>
    </div>

    <div class="field">
      <%= f.label :activated %>
      <%= f.check_box :activated %>
    </div>

    <div class="field">
      <%= f.label :activated_at %>
      <%= f.datetime_select :activated_at %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
