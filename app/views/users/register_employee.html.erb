<% provide :title ,'青松 - 普通员工注册' %>
<%
  #表单提交链接
  provide :reg_form_submit_to , "/register_employee"
  #表单提交方法
  provide :reg_submit_method , "get"

%>





<div class="container">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <h1 class="pagetitle">普通员工注册</h1>
    </div>
  </div>
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <div class="panelcontainer center">
        <%= form_tag(yield(:reg_form_submit_to),id:"normaluser_regform", method: yield(:reg_submit_method)) do %>

            <div class="input_group">
              <div class="icon_inputcontainer">
                <%= label_tag(:email,
                              content_tag(:i,nil,class: "fa fa-envelope"))
                %>
                <%= email_field_tag(:email,nil,placeholder: "邮箱(以后用于登录)") %>
              </div>
              <p class="input_errorbox left" id="err_email" data-usefor="errbox"></p>
            </div>

            <div class="input_group">
              <div class="icon_inputcontainer">
                <%= label_tag(:name,
                              content_tag(:i,nil,class: "fa fa-user"))
                %>
                <%= text_field_tag(:name,nil,placeholder: "姓名") %>
              </div>
              <p class="input_errorbox left" id="err_name" data-usefor="errbox"></p>
            </div>

            <div class="input_group">
              <div class="icon_inputcontainer">
                <%= label_tag(:password,
                              content_tag(:i,nil,class: "fa fa-lock"))
                %>
                <%= password_field_tag(:password,nil,placeholder: "密码") %>
              </div>
              <p class="input_errorbox left" id="err_password" data-usefor="errbox"></p>
            </div>

            <div class="input_group">
              <div class="icon_inputcontainer">
                <%= label_tag(:password_confirm,
                              content_tag(:i,nil,class: "fa fa-lock"))
                %>
                <%= password_field_tag(:password_confirm,nil,placeholder: "请重复输入密码") %>
              </div>
              <p class="input_errorbox left" id="err_password_confirm" data-usefor="errbox"></p>
            </div>

            <div class="input_group">
              <div class="icon_inputcontainer">
                <%= label_tag(:invitecode,
                              content_tag(:i,nil,class: "fa fa-link"))
                %>
                <%= password_field_tag(:invitecode,nil,placeholder: "邀请码") %>
              </div>
              <p class="input_errorbox left" id="err_invitecode" data-usefor="errbox"></p>
            </div>

            <%= submit_tag("创建账号",class:"btn-green choosebtn") %>
            <p class="input_errorbox center" id="err_submit" data-usefor="errbox"></p>

        <% end %>
      </div>
    </div>


    <br style="clear:both">

    <div class="col-md-offset-2 extratips">

      <h3>注册遇到困难?</h3>
      <p>
        1. 没有邀请码? 可以向贵公司的高层管理员请求邀请码.<br>
        2. 根据邀请码,将会把您的账号分配到派发此邀请码的公司底下.<br>
        3. 如果贵公司是想入驻青松平台, 可以后退此页面并选择 "公司高管" 进行注册
      </p>
    </div>

  </div>
</div>