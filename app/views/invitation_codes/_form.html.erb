<%= form_for(invitation_code, remote: true) do |f| %>
    <% if invitation_code.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(invitation_code.errors.count, "error") %> prohibited this invitation_code from being
            saved:</h2>

          <ul>
            <% invitation_code.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :company_id,'选择公司' %>
      <%= f.select :company_id, options_from_collection_for_select(Company.all, :id, :company_name) %>
    </div>
    <br>
    <div class="actions">
      <%= f.submit '生成邀请码' %>
    </div>
<% end %>
